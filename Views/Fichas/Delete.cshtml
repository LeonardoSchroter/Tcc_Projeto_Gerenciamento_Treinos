@model Projeto_gerencia_treinos_musculacao.Models.Ficha

@{
    ViewData["Title"] = "Excluir Ficha";
}

<h1 class="h3 mb-4 text-gray-800">Confirmar Exclusão</h1>

<div class="card shadow mb-4">
    <div class="card-header py-3">
        <h6 class="m-0 font-weight-bold text-danger">Atenção: Ação Irreversível</h6>
    </div>
    <div class="card-body">

        <p>Você tem certeza que deseja excluir permanentemente a ficha <strong>"@Model.Nome"</strong> do aluno <strong>@(Model.Aluno?.Nome ?? "N/A")</strong>?</p>

        <div class="alert alert-danger" role="alert">
            <h4 class="alert-heading"><i class="fas fa-exclamation-triangle"></i> Cuidado!</h4>
            <p>
                Ao excluir esta ficha, <strong>todas as divisões de treino (A, B, C...) contidos nela também serão permanentemente apagados.</strong>
            </p>
            <hr>
            <p class="mb-0">Esta ação não pode ser desfeita.</p>
        </div>

        <h5 class="mt-4">Detalhes da Ficha a ser Excluída:</h5>
        <hr />
        <dl class="row">
            <dt class="col-sm-3">
                @Html.DisplayNameFor(model => model.Nome)
            </dt>
            <dd class="col-sm-9">
                @Html.DisplayFor(model => model.Nome)
            </dd>
            <dt class="col-sm-3">
                @Html.DisplayNameFor(model => model.Aluno)
            </dt>
            <dd class="col-sm-9">
                @Html.DisplayFor(model => model.Aluno.Nome)
            </dd>
            <dt class="col-sm-3">
                @Html.DisplayNameFor(model => model.Personal)
            </dt>
            <dd class="col-sm-9">
                @Html.DisplayFor(model => model.Personal.Nome)
            </dd>
            <dt class="col-sm-3">
                Vigência
            </dt>
            <dd class="col-sm-9">
                @Model.DataInicio.ToShortDateString() a @Model.DataFim.ToShortDateString()
            </dd>
        </dl>

        <form asp-action="Delete" class="mt-4">
            <input type="hidden" asp-for="Id" />
            <input type="submit" value="Sim, Excluir Permanentemente" class="btn btn-danger" />
            <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
        </form>
    </div>
</div>